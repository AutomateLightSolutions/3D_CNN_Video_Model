# Web framework / API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
python-multipart>=0.0.9
aiofiles>=23.2.1

# Database
sqlalchemy>=2.0.0

# Core numeric / CV
numpy>=1.26.0
opencv-python>=4.9.0
scikit-learn>=1.4.0
scipy>=1.12.0
joblib>=1.3.0

# Deep learning
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.40.0
pytorchvideo>=0.1.5

# Interpretable feature extraction (feature_extractor.py)
ultralytics>=8.2.0
# mediapipe.solutions (used for pose features) is removed in mediapipe>=0.10.14 —
# pin below that line or pose features (indices 15-18) silently degrade to zero.
mediapipe==0.10.13
